import { createFileRoute } from "@tanstack/react-router";
import FontanerosPage from "@/pages/FontanerosPage";

export const Route = createFileRoute("/_main/seo-para-fontaneros")({
  component: FontanerosPage,
});
