import { createFileRoute } from "@tanstack/react-router";
import AbogadosPage from "@/pages/AbogadosPage";

export const Route = createFileRoute("/_main/seo-para-abogados")({
  component: AbogadosPage,
});
